diff -r 729e35ca6f52 Quick3way.java
--- a/Quick3way.java	Wed Dec 31 03:28:56 2014 +0900
+++ b/Quick3way.java	Wed Dec 31 03:29:43 2014 +0900
@@ -43,7 +43,6 @@
     public static void sort(Comparable[] a) {
         StdRandom.shuffle(a);
         sort(a, 0, a.length - 1);
-        assert isSorted(a);
     }
 
     // quicksort the subarray a[lo .. hi] using 3-way partitioning
@@ -62,7 +61,6 @@
         // a[lo..lt-1] < v = a[lt..gt] < a[gt+1..hi]. 
         sort(a, lo, lt-1);
         sort(a, gt+1, hi);
-        assert isSorted(a, lo, hi);
     }
 
 
@@ -117,8 +115,10 @@
      */
     public static void main(String[] args) {
         String[] a = StdIn.readAllStrings();
+        Stopwatch when = new Stopwatch();
         Quick3way.sort(a);
-        show(a);
+        if (isSorted(a)) StdOut.println("Quick3way sec. = " + when.elapsedTime());
+        else show(a);
     }
 
 }
